def type(nodeimpl)
    public
        [$aggregable
        public Definition$[$] aggregables() {
        }]
end

def !type(word) trigger(node)
    $name+firstUpperCase~Intention {
    $name+firstUpperCase~Intention$[] extensions() {
end