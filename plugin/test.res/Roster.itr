def type(Roster)
    <roster>
        $Coach
        <players>
            $Player...[$NL]
        </players>
    </roster>
end

def type(Person) trigger(Coach)
    <coach name="$Name" year="$Birthday+Year" country="$Country" />
end

def type(Person) trigger(Player)
    <player name="$Name" year="$Birthday+Year" country="$Country"[ club="$Club"]/>
end
